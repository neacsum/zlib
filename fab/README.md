This folder contains build scripts for different OS environments.

Currently, the only tested one is `win` for Microsoft Windows (64 and 32 bits).

Linux build is done using `cmake` from repository root folder.